{% extends "generic/clean_data.jinja2" %}
{% block content %}
{{ super() }}

echo "Removing subdirectories for other subjects and groups"
find $WorkingDir \
    -maxdepth 1 \
    -type d \
    -not -newer $START_FILE \
    -exec rm -rf {} \;
echo "Remaining files:"
find $StudyFolder
{% endblock content %}
